FROM python:3.10-slim

# 安装基础依赖
RUN pip install runpod

# 设置工作目录
WORKDIR /

# 复制最简化的处理器
COPY minimal_handler.py .

# 设置环境变量
ENV PYTHONUNBUFFERED=1

# 直接运行最简化处理器
CMD ["python3", "-u", "minimal_handler.py"]